---
description: Run validation and fix failures.
---
# Validate

Run the validation commands from the plan, including `go test ./...` and coverage when relevant. Fix failures before continuing. Write `$ARTIFACTS_DIR/validation.md` with commands, results, and remaining risk.
