---
description: Commit, push, and create or update the pull request.
---
# Finalize PR

Check git status carefully and stage only files changed for this workflow. Use the project PR template if present, push the branch, create or update the PR with `gh`, and write `$ARTIFACTS_DIR/pr-ready.md`, `.pr-number`, and `.pr-url`.
