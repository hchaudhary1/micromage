---
description: Prepare branch and plan context for implementation.
---
# Plan Setup

Read `$ARTIFACTS_DIR/plan.md`. Create `$ARTIFACTS_DIR/plan-context.md` with branch name, acceptance criteria, files to change, validation commands, and scope limits. Respect the repo rule to avoid NPM dependencies.
